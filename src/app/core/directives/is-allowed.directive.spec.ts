import { IsAllowedDirective } from './is-allowed.directive';

describe('IsAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
